% -*- encoding: utf-8 -*-
\documentclass[a4paper]{article}
\usepackage{luamplib}
\parindent=0mm
\begin{document}
\section{Division de \textbf{9212} par \textbf{1}, procédé standard, base \textbf{10}}

Fastoche, \textbf{9212} divisé par 1 donne \textbf{9212}, reste 0

\begin{mplibcode}
beginfig(1);
dx = 6;
dy = 10;
label(btex \bf 9 etex, (-3 * dx, -1 * dy));
label(btex \bf 2 etex, (-2 * dx, -1 * dy));
label(btex \bf 1 etex, (-1 * dx, -1 * dy));
label(btex \bf 2 etex, (0 * dx, -1 * dy));
endfig;
\end{mplibcode}
\vspace{2mm}
\end{document}
\section{Addition (base \textbf{10})}

\begin{mplibcode}
beginfig(1);
dx = 6;
dy = 10;
label(btex \bf 1 etex, (0 * dx, -1 * dy));
label(btex \bf 1 etex, (-2 * dx, 0 * dy));
label(btex \bf 3 etex, (-1 * dx, 0 * dy));
label(btex \bf 9 etex, (0 * dx, 0 * dy));
draw (-2.5 * dx, -1.5 * dy) -- (0.5 * dx, -1.5 * dy);
endfig;
\end{mplibcode}
\vspace{2mm}
\textbf{9} et \textbf{1}, \textbf{10}

Je pose \textbf{0} et je retiens \textbf{1}

\begin{mplibcode}
beginfig(1);
dx = 6;
dy = 10;
label(btex \bf 0 etex, (0 * dx, -2 * dy));
label(btex \it 1 etex, (0 * dx, -1 * dy));
label(btex  1 etex, (-2 * dx, 0 * dy));
label(btex  3 etex, (-1 * dx, 0 * dy));
label(btex \it 9 etex, (0 * dx, 0 * dy));
draw (-2.5 * dx, -1.5 * dy) -- (0.5 * dx, -1.5 * dy);
endfig;
\end{mplibcode}
\vspace{2mm}
\textbf{3} et \textbf{1}, \textbf{4}

Je pose \textbf{4} et je ne retiens rien

\begin{mplibcode}
beginfig(1);
dx = 6;
dy = 10;
label(btex \bf 4 etex, (-1 * dx, -2 * dy));
label(btex  0 etex, (0 * dx, -2 * dy));
label(btex  1 etex, (0 * dx, -1 * dy));
label(btex  1 etex, (-2 * dx, 0 * dy));
label(btex \it 3 etex, (-1 * dx, 0 * dy));
label(btex  9 etex, (0 * dx, 0 * dy));
draw (-2.5 * dx, -1.5 * dy) -- (0.5 * dx, -1.5 * dy);
endfig;
\end{mplibcode}
\vspace{2mm}
Je pose \textbf{1}

\begin{mplibcode}
beginfig(1);
dx = 6;
dy = 10;
label(btex \bf 1 etex, (-2 * dx, -2 * dy));
label(btex  4 etex, (-1 * dx, -2 * dy));
label(btex  0 etex, (0 * dx, -2 * dy));
label(btex  1 etex, (0 * dx, -1 * dy));
label(btex \it 1 etex, (-2 * dx, 0 * dy));
label(btex  3 etex, (-1 * dx, 0 * dy));
label(btex  9 etex, (0 * dx, 0 * dy));
draw (-2.5 * dx, -1.5 * dy) -- (0.5 * dx, -1.5 * dy);
endfig;
\end{mplibcode}
\vspace{2mm}
